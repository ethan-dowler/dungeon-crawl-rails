<%= render HeadingComponent.new(title: "Characters") %>

<div class="TileSelect">
  <% characters.each do |character| %>
    <div class="TileSelect-tile">
      <div class="TileSelect-level">
        Lvl. <%= character.level %>
      </div>
      <div class="TileSelect-name">
        <%= character.name %>
      </div>

      <%= link_to "Select", character_path(character), class: "Button Button--full" %>
    </div>
  <% end %>
</div>

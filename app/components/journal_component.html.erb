<%= turbo_frame_tag :journal do %>
  <div
    class="Journal"
  >
    <% event_logs.each do |event_log| %>
      <%= render EventLogComponent.new(event_log) %>
    <% end %>
    <div class="Journal-more">
      ...
    </div>
    <%# TODO: add "see full log" button to see more than just the most recent logs %>
  </div>
<% end %>
